{
  "name": "verify-hono-oauth-providers",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "check": "biome check . --unsafe --write"
  },
  "dependencies": {
    "@hono/oauth-providers": "^0.5.0",
    "hono": "^4.4.7"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.8.2",
    "@types/bun": "latest"
  }
}
